print(default_api.natural_language_write_file(language = "typescript", path = "src/utils/cn.ts", prompt = "In typescript, Create a TypeScript function named `cn` that accepts a dynamic number of arguments. These arguments can be strings, falsy values (like `null`, `undefined`, or `false`), or objects. The function should: 1.  Ignore any falsy values. 2.  Include strings as class names. 3.  For objects, treat the object keys as class names and the object values as boolean conditions: if a value is `true`, include the key as a class name; otherwise, omit it. The function should then return a single string that concatenates all the valid class names, separated by spaces."))